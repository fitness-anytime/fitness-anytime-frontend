import React from "react";
import { FormControl, MenuItem, InputLabel, Select } from "@mui/material";

export default function ClassForm() {
  return (
    <div>
      <form>
        here is the create class from
        {/* 
          --Create Class Header--

          --Name Input (type=text)--
          --Type Input (dropdown(?))--
          --Start Time Input (dropdown for time slots? must be 0000-2400)
          --Duration (dropdown)
          --Intensity Input (dropdown)
          --Location Input (text)--
          --Max Capacity (dropdown?)--

          --Submit Form Button--


          
        */}
      </form>
    </div>
  );
}
